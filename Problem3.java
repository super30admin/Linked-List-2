/*
Delete the node without head pointer
approach: we just have to delete the value. so, we could just copy next value into current value and
 make curr.next = curr.next.next.
 */
public class Problem3 {
    /*void deleteNode(Node del)
    {
        // Your code here
        del.data = del.next.data;
        del.next = del.next.next;
    }*/
}
