/*
def deleteNode(curr_node):
    temp = curr_node
    temp = temp.next
    while temp.next is not None:
        curr_node.data = temp.data
        curr_node = curr_node.next
        temp = temp.next
    curr_node.data = temp.data
    curr_node.next = None
*/
